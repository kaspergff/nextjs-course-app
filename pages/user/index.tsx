import Layout from "@/components/Layout"
import { FC } from "react"

interface Props {}

const userIndex: FC<Props> = ({}) => {
  return (
    <Layout>
      <p>Hoi</p>
    </Layout>
  )
}

export default userIndex
